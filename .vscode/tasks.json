{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build with mingw32-make",
            "type": "shell",
            "command": "mingw32-make",
            "args": [
				"-C","${workspaceFolder}\\build",
                "-j4"  // Optional: Adjust the number of parallel jobs
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": ["$gcc"],  // Use the default GCC problem matcher
            "detail": "Generated task for mingw32-make"
        }
    ]
}
